doctype html
head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')

    meta(property='og:title' content=title)
    meta(property='og:image' content="https://easymeme69.com" + imagePath)
    meta(property='og:site_name' content='EasyMeme')

    link(rel='icon' href='images/favicon.ico')

    title #{title}

body
    img(src=imagePath)